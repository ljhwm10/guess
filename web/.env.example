# 前端连接的后端地址。
# 留空 = 同源(前端由后端一并托管、或走 nginx/caddy 反代到同一域名时,用这个,最省事)。
# 拆分部署(前端静态托管在 A,后端 socket.io 单独跑在 B)时,填后端可访问地址:
#   VITE_SERVER_URL=https://guess-api.example.com
# 改完需要重新 `pnpm build`。若想改地址不重打包,见 DEPLOY.md 里的运行时注入方案。
VITE_SERVER_URL=
